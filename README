The tracking directory is going to contain the files that are for monitoring.  So it's everything that follows from the email inbox and the size and distrobution of the task file


The nextActions directory will be about adding nextActions, altering them, sorting them and so on. 
